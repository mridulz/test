- name: Add Oracle SID results to report
  ansible.builtin.set_fact:
    oracle_report: >-
      {{
        (oracle_report | default([]))
        +
        [{
          'oracle_sid': oracle_sid,
          'opatch_list': opatch_result.stdout
        }]
      }}

{% for host in ansible_play_hosts %}

  <h2>{{ host }}</h2>

  <table>
    <thead>
      <tr>
        <th>Oracle SID</th>
        <th>Ordered Inactive Patches</th>
      </tr>
    </thead>

    <tbody>
      {% for oracle in hostvars[host].oracle_report %}
      <tr>
        <td>{{ oracle.oracle_sid }}</td>
        <td>
          <pre>{{ oracle.opatch_list }}</pre>
        </td>
      </tr>
      {% endfor %}
    </tbody>
  </table>

{% endfor %}


