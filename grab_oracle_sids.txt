- name: Add SID results to Oracle report
  ansible.builtin.set_fact:
    oracle_report: >-
      {{
        oracle_report | default({}) |
        combine({
          inventory_hostname: {
            oracle_sid: {
              'opatch_list': opatch_result.stdout
            }
          }
        }, recursive=True)
      }}


